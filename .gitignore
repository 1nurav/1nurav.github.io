dist/
node_modules/
.astro/
.DS_Store
*.log

# Full-resolution photo masters. Kept locally, too heavy to ship.
originals/

# Session transcripts. Local reference only, never publishable:
# this repo is public and the transcripts contain work email addresses and
# internal system names.
*.jsonl
session-*.json
