---
import { marquee } from '../data/site.js';

// Track is duplicated so the -50% translate loops seamlessly.
const track = [...marquee, ...marquee];
---

{/* Decorative. The list is duplicated for the loop and padded with asterisks, so
    a screen reader would otherwise read "team lead, star, unblocker of blockers,
    star..." and then the whole thing a second time. */}
<div class="strip" aria-hidden="true">
  <div class="track">
    <span class="run">
      {track.map((word) => (
        <Fragment>
          <span>{word}</span>
          <span>*</span>
        </Fragment>
      ))}
    </span>
  </div>
</div>

<style>
  .strip {
    position: relative;
    z-index: 2;
    overflow: hidden;
    padding: 16px 0;
    background: var(--acc);
    color: var(--bg);
    border-top: 1px solid rgb(var(--line-rgb) / 0.08);
    border-bottom: 1px solid rgb(var(--line-rgb) / 0.08);
  }
  .track {
    display: flex;
    width: max-content;
    animation: marquee 22s linear infinite;
  }
  .run {
    display: flex;
    gap: 34px;
    padding-right: 34px;
    font-family: var(--display);
    font-weight: 700;
    font-size: clamp(20px, 2.6vw, 38px);
    letter-spacing: -0.02em;
    text-transform: uppercase;
  }
</style>
